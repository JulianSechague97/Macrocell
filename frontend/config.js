// Configuración global del backend
// Asegúrate de que esta URL sea la que te da ngrok (sin espacios ni prefijos)
const API_URL = "https://aliza-falser-mui.ngrok-free.dev".trim();

// Depuración rápida: mostrar la URL que usará el frontend
console.log("Frontend usando API_URL =", API_URL);
